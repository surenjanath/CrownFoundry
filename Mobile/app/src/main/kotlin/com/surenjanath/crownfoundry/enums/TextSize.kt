package com.surenjanath.crownfoundry.enums

/** How large every text in the app is drawn, relative to the design's own sizes. */
enum class TextSize(val scale: Float) {
    Small(0.9f),
    Medium(1f),
    Large(1.1f),
    Huge(1.25f)
}
