<?xml version="1.0" encoding="utf-8"?>
<!--
    The referee normally runs on the developer's own machine over plain HTTP: 10.0.2.2 is the
    host as the emulator sees it, and a private-range address is a phone on the same Wi-Fi.
    Everything else still requires TLS.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>

    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
