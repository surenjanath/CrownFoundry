# Copy to keystore.properties and fill in. Never commit the real one - it is gitignored.
#
#   keytool -genkeypair -v -keystore release.jks -alias wye \
#       -keyalg RSA -keysize 4096 -validity 10000
#
storeFile=release.jks
storePassword=
keyAlias=wye
keyPassword=
